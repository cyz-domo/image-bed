import { githubUser, publicOrigin, sessionCookie, sessionValue } from "../../_lib/auth.js";
import { error, getCookie } from "../../_lib/http.js";
export async function onRequest({ request, env }) { const url = new URL(request.url); const code = url.searchParams.get("code"); const state = url.searchParams.get("state"); if (!code || !state || state !== getCookie(request, "oauth_state")) return error("OAUTH_STATE_INVALID", "登录状态已失效，请重试", 400); try { const user = await githubUser(code, `${publicOrigin(request, env)}/api/auth/callback`, env); if (user.login.toLowerCase() !== env.ALLOWED_GITHUB_LOGIN.toLowerCase()) return error("ACCOUNT_NOT_ALLOWED", "当前 GitHub 账号没有上传权限", 403); const headers = new Headers({ Location: "/", "Cache-Control": "no-store" }); headers.append("Set-Cookie", sessionCookie(await sessionValue(user.login, env))); return new Response(null, { status: 302, headers }); } catch (cause) { return error("OAUTH_FAILED", cause.message, 502); } }
